# 🍌 Banana Bot — Telegram-бот для Google Gemini

Быстрый и безопасный Telegram-бот для взаимодействия с мощными моделями **Google Gemini**.

💡 **Главные особенности:**
- **Stateless-архитектура:** Бот не сохраняет контекст переписки в памяти, что значительно ускоряет работу и экономит лимиты API.
- **Интуитивный интерфейс:** Жестко заданные кнопки с дружелюбными текстами делают использование бота простым и понятным даже для новичков.
- **Интеллектуальная обработка:** Бот принимает строго нужный формат данных (фото/текст/аудио) и корректно сообщает об ошибках.
- **Защита от сбоев:** Предусмотрена обработка ошибок API и возможность отменить любой начатый процесс (через меню). 
- **Легкий доступ к ИИ:** Бот позволяет пользоваться мощностями моделей Gemini прямо из мессенджера Telegram, без VPN и сложных интерфейсов.

---

## 🛠 Возможности

| Ввод / Кнопка | Описание |
|-------------------|-----------------|
| 🎨 **Создать шедевр** | Генерирует уникальную картинку по вашему текстовому или голосовому запросу |
| 🪄 **Преобразить фото** | Редактирует отправленную вами фотографию по инструкции (текстом или голосом) |
| 💡 **Подсказка** | Выводит краткую справку по функционалу бота |
| ⚡️ / 💎 **Режимы** | Переключайтесь между режимами: максимальное качество или мгновенная скорость |

---

## 🧠 Доступные режимы (Модели нейросети)

У пользователя есть выбор между двумя режимами в зависимости от нужд:

| Режим | Описание | Модели для генерации |
|-------|---------|-------------------|
| 💎 **PRO** | Подробный режим, лучшая детализация изображений и максимальный интеллект текстов | `gemini-3-pro-image-preview` <br> `gemini-3.1-pro-preview` |
| ⚡️ **FLASH** | Турборежим: генерация и ответы почти мгновенно, идеально для быстрых набросков | `gemini-2.5-flash-image` <br> `gemini-3-flash-preview` |

---

## 🚀 Установка и запуск

Склонируйте репозиторий и перейдите в его директорию:
```bash
git clone <url> && cd banana-bot
```

### 1. Подготовка окружения (Рекомендуется)

Создайте и активируйте виртуальное окружение:

**Для Mac/Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**Для Windows:**
```bash
python -m venv venv
.\venv\Scripts\activate
```

Установите необходимые библиотеки:
```bash
pip install -r requirements.txt
```

### 2. Заполнение конфига

Создайте файл `.env` в корневой папке проекта с переменными окружения:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_@BotFather
GOOGLE_API_KEY=ваш_ключ_из_Google_AI_Studio
ALLOWED_USERS=123456789,987654321

# Опционально: Для серверов (Render, Railway, Heroku и т.д.)
WEBHOOK_URL=https://ваша-ссылка.com
PORT=8080
```

### 3. Запуск бота

Обязательно убедитесь, что виртуальное окружение активировано, и запустите файл:
```bash
python bot.py
```

> **Примечание:** Бот автоматически использует привычный метод *Polling*, если запускается локально. Если в `.env` прописан `WEBHOOK_URL`, бот запустит сервер на указанном порту для работы через *Webhook*.