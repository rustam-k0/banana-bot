# 🍌 Banana Bot — Telegram-бот для Google Gemini

Быстрый и секьюрный Telegram-бот для взаимодействия с **Google Gemini**.
💡 **Особенность:** Бот работает "stateless" (без сохранения контекста переписки) и с жёстко заданными текстами кнопок. Это существенно экономит вызовы к API (лимиты токенов). Бот никогда не отправляет текст вместо изображения и не позволяет отправлять себе текст вместо фото. Бот сделан для нетехнических пользователей и работает максимально простым и интуитивным образом. Бот позволяет выбрать, к какой модели обращается пользователь: к дорогой pro или быстрой flash. Бот отправляет по api в llm только сущностные запросы, а любые взаимодействия с пользователем обрабатывает через хардкод кнопок с кратким и дружелюбным текстом. Бот обрабатывает ошибки, имеет возможность вернуться домой или отменить текущую ветку диалогов. Этот бот помогает людям без доступа к ллм пользоваться гемини через интерфейс телеграм. 

## Функции

## Функции

| Ввод / Кнопка | Описание |
|-------------------|-----------------|
| 🎨 **Нарисовать** | Нарисует картинку по вашему тексту |
| 🪄 **Изменить** | Изменит отправленное фото по вашему описанию в подписи |
| ℹ️ **Справка** | Краткая инструкция по боту |
| 🚀 / 💎 **Режимы** | Переключение между быстрым и качественным режимами |

## Режимы нейросети

| Режим | Описание | Модели для генерации |
|-------|---------|-------------------|
| 💎 **PRO** | Подробный режим, лучшая детализация | `gemini-3-pro-image-preview` |
| 🚀 **FLASH** | Максимальная скорость для быстрой работы | `gemini-2.5-flash-image` |

## Установка и запуск

1. Склонируйте репозиторий и перейдите в папку:
```bash
git clone <url> && cd banana-bot
```

2. Создайте и активируйте виртуальное окружение (рекомендуется):
```bash
python3 -m venv venv
source venv/bin/activate  # Для Mac/Linux
# .\venv\Scripts\activate # Для Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` в папке с ботом и заполните его:
```env
TELEGRAM_BOT_TOKEN=токен_от_@BotFather
GOOGLE_API_KEY=ключ_из_Google_AI_Studio
ALLOWED_USERS=123456789,987654321

# Опционально: если загружаете на сервер (Render, Railway)
WEBHOOK_URL=https://ваша-ссылка.com
PORT=8080
```

5. Запустите бота (убедитесь, что виртуальное окружение активировано):
```bash
source venv/bin/activate
python3 bot.py
```
*(Бот автоматически использует обычный запуск (Polling) для ПК, или Webhook, если указана ссылка на сервер в `WEBHOOK_URL`).*