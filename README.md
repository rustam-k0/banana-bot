# Banana Bot 🍌 — Telegram-бот для Google Gemini

Приватный Telegram-бот, который даёт доступ к мультимодальной нейросети **Google Gemini** через удобный интерфейс с кнопками на русском языке.

## Что умеет бот

| Функция | Как использовать |
|---------|-----------------|
| 🤖 **Текстовый ИИ** | Просто напишите любой вопрос |
| 🎤 **Голосовые сообщения** | Отправьте голосовое — бот расшифрует и ответит |
| 📷 **Анализ фотографий** | Отправьте фото — бот расскажет что на нём |
| 🎨 **Генерация картинок** | Кнопка «Сгенерировать картинку» + описание |
| 🪄 **Редактирование фото** | Кнопка «Изменить фото» → фото → описание изменений |

## Режимы работы

Переключаются кнопкой прямо в Telegram, без перезапуска:

| Режим | Текст / Голос / Фото | Генерация картинок |
|-------|---------------------|-------------------|
| 💎 **PRO** | `gemini-2.5-pro` → `gemini-2.5-flash` | `gemini-2.5-flash-image` |
| 🚀 **FLASH** | `gemini-2.5-flash` → `gemini-2.5-flash-lite` | `gemini-2.5-flash-image` |

При временных ошибках API (429, 500, 503) бот автоматически переключается на следующую модель в каскаде.

## Стек

- Python 3.10+
- `aiogram 3.x` — Telegram Bot API
- `aiohttp` — Webhook-сервер
- `google-genai` ≥ 1.0.0 — Google GenAI SDK

## Установка и запуск

### 1. Клонируйте репозиторий

```bash
git clone <url> && cd banana-bot
pip install -r requirements.txt
```

### 2. Настройте `.env`

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
GOOGLE_API_KEY=ваш_ключ_из_Google_AI_Studio
ALLOWED_USERS=123456789,987654321

# Опционально — для деплоя через webhook:
# WEBHOOK_URL=https://your-app.onrender.com
# PORT=8080
```

### 3. Запустите

```bash
python bot.py
```

- Без `WEBHOOK_URL` → бот работает в **polling-режиме** (удобно для локальной разработки)
- С `WEBHOOK_URL` → бот работает через **webhook** (для серверного деплоя: Render, Railway, Heroku)

## Переменные окружения

| Переменная | Обязательная | Описание |
|-----------|:---:|---------|
| `TELEGRAM_BOT_TOKEN` | ✅ | Токен бота от @BotFather |
| `GOOGLE_API_KEY` | ✅ | API-ключ Google AI Studio |
| `ALLOWED_USERS` | ✅ | ID пользователей через запятую |
| `WEBHOOK_URL` | ❌ | Публичный URL для webhook |
| `PORT` | ❌ | Порт сервера (по умолчанию 8080) |