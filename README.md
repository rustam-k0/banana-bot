# Banana Telegram Bot (Gemini AI Proxy)

Асинхронный Telegram-бот, работающий как прокси для актуального Google Gemini API. Ориентирован на максимальную простоту интерфейса при сохранении высокой скорости работы.

## Стек и модели
* **Фреймворк:** `aiogram>=3.4.1`, `aiohttp` (для healthcheck порта)
* **API Клиент:** `google-genai>=0.5.0` (Асинхронные вызовы `client.aio`)
* **Текст и Vision:** `gemini-2.5-flash`
* **Генерация изображений:** `gemini-2.5-flash-image`

## Возможности
* **💬 Поговорить:** Контекстный диалог и выполнение текстовых задач.
* **🎨 Нарисовать:** Генерация изображений по текстовому описанию (соотношение 1:1).
* **👁️ Посмотреть фото:** Анализ изображений (Vision). Бот отвечает на вопросы по загруженной фотографии.
* **🖌️ Изменить фото:** Редактирование загруженного изображения по промпту.
* **🔒 Приватность:** Доступ строго ограничен по списку Telegram ID.

## Установка и запуск (Локально)

### 1. Подготовка окружения
Создай файл `.env` в корневой директории проекта:

```env
TELEGRAM_BOT_TOKEN=твой_токен_от_botfather
GOOGLE_API_KEY=твой_ключ_gemini
ALLOWED_USERS=123456789,987654321
PORT=8080

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python bot.py