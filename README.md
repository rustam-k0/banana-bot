# Gemini Telegram Bot

🔗 Простой, асинхронный и функциональный Telegram-бот для использования Google Gemini API через Webhooks. Поддерживает работу с текстом, голосовыми и фото сообщениями, а также генерацию изображений.

## Функционал

* **🤖 Текст & Режим диалога:** Отправка быстрых текстовых запросов к последним моделям Gemini.
* **👀 Анализ фото:** Возможность отправить картинку в бота и попросить её описать или проанализировать.
* **🎧 Голосовые сообщения & Аудио:** Бот слушает голосовые и делает автоматическую транскрибацию с ответом.
* **🎨 Генерация изображений:** Встроенная поддержка генерации изображений через `imagen-3.0`.
* **🧠 Каскадизация и отказоустойчивость:** Автоматический переход к запасным моделям если основная перегружена (отлов 429, 503 ошибок).
* **Русскоязычный интерфейс** с кнопками.

## Архитектура моделей (Умный Каскад)
Маршрутизация переключается напрямую в интерфейсе Telegram:
* `💎 Режим: PRO` - Максимальное качество и точность логики:
  * Текст / Voice / Vision: `gemini-2.5-pro` -> `gemini-2.5-flash` -> `gemini-1.5-pro`
  * Изображения: `imagen-3.0-generate-002` -> `imagen-3.0-generate-001`
* `🚀 Режим: FLASH` - Высокая скорость и низкая стоимость (Топ по умолчанию):
  * Текст / Voice / Vision: `gemini-2.5-flash` -> `gemini-1.5-flash`
  * Изображения: `imagen-3.0-generate-001` -> `imagen-3.0-fast-001`

При крашах бота умный каскад всегда выбирает резервную модель.

## Стек и Зависимости
* Python 3.10+
* `aiogram 3.x`, `aiohttp` (для Webhooks)
* `google-genai` (Официальный Google SDK)

## Развертывание
1. Форкни / Склонируй репозиторий.
2. Пропиши все необходимые переменные: `TELEGRAM_BOT_TOKEN`, `GOOGLE_API_KEY`, `ALLOWED_USERS` (ID юзеров через запятую) и `WEBHOOK_URL` в переменные окружения вашей панели управления (например Render, Railway или в локальном `.env` файле).
3. Установи зависимости: `pip install -r requirements.txt`
4. Запусти бота: `python bot.py`